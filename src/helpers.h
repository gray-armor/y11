#ifndef Y11_HELPERS_H
#define Y11_HELPERS_H

#ifndef MIN
#define MIN(x, y) (((x) < (y)) ? (x) : (y))
#endif

#ifndef MAX
#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#endif

#endif /* Y11_HELPERS_H */
